#include<iostream>
#include<algorithm>
using namespace std;
void permute(string str)
{
    sort(str.begin(),str.end());
    
    do
    {
        cout<<str<<" ";
    }while(next_permutation(str.begin(),str.end()));
}
int main()
 {
	//code
	int t;
	cin>>t;
	while(t--)
	{
	    string str;
	    cin>>str;
	    
	    permute(str);
	    cout<<endl;
	}
	return 0;
}
